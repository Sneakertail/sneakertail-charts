apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp-route
  namespace: kube-system
spec:
  parentRefs:
  - name: kgateway
    namespace: dev
  hostnames:
  - "headlamp.sneakertail.online"
  rules:
  - backendRefs:
    - name: my-headlamp
      port: 80
